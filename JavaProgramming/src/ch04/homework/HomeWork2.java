package ch04.homework;

public class HomeWork2 {

	public static void main(String[] args) {
		
		for(int x=1;x<=10;x++){                          //x는 1에서 10까지 반복하며 대입한다.
			int y = 1;									 // y에 1을 대입한다.
			if(4*x+5*y==60){							 // 조건식이 true면
				System.out.println("("+x+","+y+")");	 // (x,y)를 출력한다.
				
			}
		}
		for(int x=1;x<=10;x++){							 //x는 1에서 10까지 반복하며 대입한다.
			int y = 2;								   	// y에 2을 대입한다.
			if(4*x+5*y==60){							// 조건식이 true면
				System.out.println("("+x+","+y+")");   // (x,y)를 출력한다.
		
			}
		}
		for(int x=1;x<=10;x++){							 //x는 1에서 10까지 반복하며 대입한다.
			int y = 3;									 // y에 3을 대입한다.
			if(4*x+5*y==60){							// 조건식이 true면
				System.out.println("("+x+","+y+")");	// (x,y)를 출력한다.
			
			}
		}
		for(int x=1;x<=10;x++){							//x는 1에서 10까지 반복하며 대입한다.
			int y = 4;									// y에 4을 대입한다.
			if(4*x+5*y==60){							// 조건식이 true면
				System.out.println("("+x+","+y+")");	// (x,y)를 출력한다.
			
			}
		}
		for(int x=1;x<=10;x++){							//x는 1에서 10까지 반복하며 대입한다.
			int y = 5;									// y에 5을 대입한다.
			if(4*x+5*y==60){							// 조건식이 true면
				System.out.println("("+x+","+y+")");	// (x,y)를 출력한다.
			
			}
		}
		for(int x=1;x<=10;x++){							//x는 1에서 10까지 반복하며 대입한다.
			int y = 6;									// y에 6을 대입한다.
			if(4*x+5*y==60){							// 조건식이 true면
				System.out.println("("+x+","+y+")");	// (x,y)를 출력한다.
			
			}
		}
		for(int x=1;x<=10;x++){							//x는 1에서 10까지 반복하며 대입한다.
			int y = 7;									// y에 7을 대입한다.
			if(4*x+5*y==60){							// 조건식이 true면
				System.out.println("("+x+","+y+")");	// (x,y)를 출력한다.
			
			}
		}
		for(int x=1;x<=10;x++){							//x는 1에서 10까지 반복하며 대입한다.
			int y = 8;									// y에 8을 대입한다.
			if(4*x+5*y==60){							// 조건식이 true면
				System.out.println("("+x+","+y+")");	// (x,y)를 출력한다.
			
			}
		}
		for(int x=1;x<=10;x++){							//x는 1에서 10까지 반복하며 대입한다.
			int y = 9;									// y에 9을 대입한다.
			if(4*x+5*y==60){							// 조건식이 true면
				System.out.println("("+x+","+y+")");	// (x,y)를 출력한다.
			
			}
		}
		for(int x=1;x<=10;x++){							//x는 1에서 10까지 반복하며 대입한다.
			int y = 10;									// y에 10을 대입한다.
			if(4*x+5*y==60){							// 조건식이 true면
				System.out.println("("+x+","+y+")");	// (x,y)를 출력한다.
			
			}
		}
	}	
}


